### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String!
  user: User!
}

type Card {
  answer: String!
  cardId: Int!
  category: String!
  isDone: Boolean!
  postedBy: User
  question: String!
}

type Mutation {
  delete(cardId: Int!): Card!
  login(email: String!, password: String!): AuthPayload!
  markAsDone(cardId: Int!): Card!
  post(answer: String!, category: String!, question: String!): Card!
  signup(email: String!, name: String!, password: String!): AuthPayload!
  update(answer: String!, cardId: Int!, category: String!, question: String!): Card!
}

type Query {
  feed(category: String): [Card!]!
  feedById(cardId: Int!): Card
  getDoneCards(isDone: Boolean): [Card!]!
}

type User {
  cards: [Card!]!
  email: String!
  id: Int!
  name: String!
}